
//initialState of the reducer (it has only user:null)

const initialState = {
    user: null
}

//action types
const LOGIN = "LOGIN"

const LOGOUT = "LOGOUT"

//defining the reducer (a function with a state and action argument that returns state)

export default (state = initialState, action ) = {
    //change state based one action.type


}